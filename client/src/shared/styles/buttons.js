import { css } from 'styled-components/macro';

// TODO: create button variant styles and use in seperate button variant components (PrimaryLighten, BorderedInset etc.) in a ButtonVariants.js file or as variants in the button component (variant = 'primary-lighten' etc. or just 'lighten', 'inset' etc. ?)

export const primaryDarken = css``;
